# Dual-VESC PWM Router PlatformIO Project

This is the PlatformIO build of `../Arduino_tow_buggie_dual_vesc/srxl2_dual_vesc`.
See `../README.md` for the wiring diagram and operating behavior.

Build it with:

```text
C:\Users\Romain\.platformio\penv\Scripts\pio.exe run
```

Upload it to the Arduino Mega on `COM5` with:

```text
C:\Users\Romain\.platformio\penv\Scripts\pio.exe run --target upload
```
